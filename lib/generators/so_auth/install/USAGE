Description:
    Generate config and initializer for a client of CommutybleAuthProvider.

Example:
    rails generate commutyble_auth:install

    This will create:
        config/initializers/omniauth.rb
